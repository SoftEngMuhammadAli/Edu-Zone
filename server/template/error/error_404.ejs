<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 Not Found</title>
    <style>
      body {
        font-family: "Segoe UI", Arial, sans-serif;
        background: linear-gradient(135deg, #f7f7f7 0%, #e0eafc 100%);
        text-align: center;
        padding: 100px 20px;
        min-height: 100vh;
        margin: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      .error-container {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 8px 32px rgba(44, 62, 80, 0.08);
        padding: 48px 32px;
        max-width: 420px;
        width: 100%;
        margin: auto;
        animation: fadeIn 1s;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      h1 {
        font-size: 6rem;
        color: #e74c3c;
        margin: 0 0 12px 0;
        letter-spacing: 8px;
        font-weight: 900;
        text-shadow: 2px 4px 16px #f8d7da;
      }
      .icon {
        font-size: 3rem;
        color: #e74c3c;
        margin-bottom: 12px;
        display: block;
        animation: shake 1.2s infinite alternate;
      }
      @keyframes shake {
        0% {
          transform: rotate(-5deg);
        }
        100% {
          transform: rotate(5deg);
        }
      }
      p {
        font-size: 1.25rem;
        color: #555;
        margin: 12px 0 24px 0;
      }
      code {
        background: #f3f3f3;
        border-radius: 4px;
        padding: 2px 6px;
        font-size: 1rem;
        color: #c0392b;
      }
      a {
        display: inline-block;
        text-decoration: none;
        color: #fff;
        background: linear-gradient(90deg, #3498db 60%, #6dd5fa 100%);
        font-weight: bold;
        padding: 12px 32px;
        border-radius: 24px;
        box-shadow: 0 2px 8px rgba(52, 152, 219, 0.12);
        transition: background 0.2s, transform 0.2s;
        font-size: 1.1rem;
      }
      a:hover {
        background: linear-gradient(90deg, #2980b9 60%, #57c1eb 100%);
        transform: translateY(-2px) scale(1.03);
      }
    </style>
  </head>
  <body>
    <div class="error-container">
      <span class="icon">🚫</span>
      <h1>404</h1>
      <p>Oops! The page <code><%= path %></code> does not exist.</p>
      <a href="/">Go back home</a>
    </div>
  </body>
</html>
